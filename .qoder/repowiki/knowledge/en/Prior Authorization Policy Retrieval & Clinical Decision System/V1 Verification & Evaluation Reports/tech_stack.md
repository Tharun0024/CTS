Reports are generated by Python scripts using FAISS and BM25 vector indices, BAAI embedding/reranker models (`BAAI/bge-base-en-v1.5`, `BAAI/bge-reranker-base`), and LLM providers via NVIDIA NIM (`integrate.api.nvidia.com/v1/chat/completions`) and OpenAI (`api.openai.com/v1`); environment variables include `NVIDIA_API_KEY`, `LLM_API_KEY`, `LLM_MODEL`, and `NVIDIA_MODEL`.