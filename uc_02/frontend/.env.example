# Frontend API base URL. Defaults to /api, which the Vite dev server proxies
# to http://localhost:8000 (see vite.config.ts). Point this at any deployment
# of the FastAPI backend; the frontend never talks to anything else.
VITE_API_BASE_URL=/api
